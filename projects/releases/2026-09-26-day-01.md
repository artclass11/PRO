# Day 01 Release — PulseCheck

Status: shipped

PulseCheck is a small HTTP/TLS health-check CLI with JSON output, latency measurement, redirect-aware URLs, TLS certificate expiry visibility, automated testing, and GitHub Actions CI.

Local verification: the core unit test passed. A live external network smoke test was not treated as a release gate because the execution environment could not resolve external DNS.
